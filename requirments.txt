# ─────────────────────────────────────────────────────────────────────────────
# NEXUS COMMAND — Python Dependency Reference
# ─────────────────────────────────────────────────────────────────────────────
# This file is for reference ONLY.
#
# The canonical dependency manifest is:
#   backend/pyproject.toml   (managed by `uv`)
#
# To install all dependencies, run from the backend/ directory:
#   uv sync
#
# To add a new package:
#   cd backend && uv add <package-name>
#
# ─────────────────────────────────────────────────────────────────────────────
# Snapshot of key dependencies (see pyproject.toml for exact pinned versions):
#
#   fastapi>=0.135.1          — API framework + SSE streaming
#   langchain>=1.2.10         — LLM orchestration base
#   langchain-google-genai    — Gemini 2.0 Flash integration
#   langchain-openai          — Groq (Llama 3.3 70B) routing
#   langchain-community       — Tavily search tool
#   langgraph>=1.0.9          — StateGraph supervisor mesh
#   langfuse>=3.14.5          — LLM observability & tracing
#   supabase>=2.28.0          — Dual-write persistence layer
#   slowapi>=0.1.9            — Rate limiting (10 req/min per IP)
#   slack-sdk>=3.40.1         — Slack Socket Mode integration
#   tavily-python>=0.7.22     — Web search for Research Agent
#   psutil>=7.2.2             — System diagnostics for Nexus Node
#   uvicorn[standard]>=0.41.0 — ASGI server
#   python-dotenv>=1.2.1      — Environment variable loading
#   pydantic>=2.12.5          — Request/response validation
# ─────────────────────────────────────────────────────────────────────────────
